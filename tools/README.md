# Tools

This directory holds scripts and utilities for the new target path.

Suggested contents:

- `build.sh` — local build entry point
- `flash.sh` — board flashing helper
- `monitor.sh` — serial monitor utility
- `env.sh` — environment setup helper

Keep helper scripts simple, repeatable, and device-specific.
